# Copy to .env (repo root) for `docker compose` mobile service.
# Use your Mac's Wi‑Fi IP so Expo Go on Android can reach Metro + API.

EXPO_PUBLIC_API_URL=http://192.168.0.132:8000
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.0.132
